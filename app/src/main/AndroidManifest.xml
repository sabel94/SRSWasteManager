<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.srswastemanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CityWasteFractionsActivity"></activity>
        <activity android:name=".CityTotalWasteActivity" />
        <activity android:name=".UserCostActivity" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".AverageCitizenActivity" />
        <activity android:name=".FindWasteStationActivity" />
        <activity android:name=".NewsletterActivity" />
        <activity android:name=".PayActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>